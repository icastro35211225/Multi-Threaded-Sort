C file assign4.c implements the sorting of a random double array uwing one thread. 
Then splits it in half and sorts the 2 halves in seperate threads. Lastly, uses one more thread
to merge them togehter.

Java file part2.java does the same just using Java's threading.
